type: object
properties:
  data:
    type: array
    items:
      type: object
      properties:
        urlId:
          $ref: ./UrlId.yaml
        title:
          $ref: ./EntityTitle.yaml
required:
  - data